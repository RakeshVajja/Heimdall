{
  "name": "heimdall-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Heimdall — Autonomous AI Agent Platform (TypeScript, Multi-LLM, MCP, Skills, Memory, Evals)",
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"SERVER,CLIENT\" -c \"cyan.bold,green.bold\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=@heimdall/shared && npm run build --workspace=server && npm run build --workspace=client",
    "lint": "npm run lint --workspaces",
    "clean": "rm -rf node_modules packages/*/node_modules server/node_modules client/node_modules client/.next"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
